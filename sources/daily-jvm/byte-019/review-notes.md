# Byte 019 review notes

- Integrated Lesson 19, `platform-vs-virtual-threads`, Threads and Synchronization, order 190, prerequisite `thread-local-handshakes`. Local draft; no publication, commit or push.
- Archived the complete supplied byte and the available reviewed-draft prefix from the originating conversation. The reader caps one message at 20,000 characters, truncating the draft during reasoning checks. Explicit user requirements supply the remaining comparison/reasoning/next-lesson contract; the excerpt is not presented as complete.
- Preserved identity/lifetime versus carrier reuse, supported mount/unmount and possible migration, continuation-backed dynamic heap stack chunks, CPU versus concurrency, ThreadLocal/name/interrupt semantics, carrier-level TLAB machinery, and downstream-resource constraints.
- Corrected the source's too-strong “mounted only while it needs CPU execution”: a mounted carrier can be descheduled or captured by unsupported blocking. Also, a virtual thread may return to the same carrier; migration is possible rather than required.
- JEP 491 shipped in JDK 24 and applies to JDK 25. Ordinary monitor ownership no longer inherently pins; native-frame interaction remains a boundary. Carrier release does not release a monitor held across sleep/I/O. Object.wait has separate monitor-release semantics.
- Comparison avoids implying every platform thread is a carrier. Resource note distinguishes concurrency limits, rate limits and admission/backlog; a DB pool already constrains checked-out connections.
- Three marks, two requested semantic callouts, eleven original linked SVGs, four reasoning disclosures and the required collapsed sources disclosure. Figures retain the course's fixed light artwork in dark mode.
- Executed the bounded identity example on Homebrew OpenJDK HotSpot 25.0.2 with a 64 MB heap. Output is archived. Request counts, carrier transitions and memory diagrams are illustrative, not measurements.
- Primary research: JEP 444 search-index design text; JDK 25 Virtual Threads guide and Thread API; JDK 24 release notes confirming JEP 491; tagged `jdk-25-ga` VirtualThread.java and HotSpot thread.hpp. Direct JEP pages returned 403 in the research tool, so version-sensitive claims were cross-checked with Oracle release documentation and JDK 25 documentation.
- Beginner audit: request handler supplies motivation; carrier, mounting, continuation, stack chunk, concurrency, parallelism, pinning and monitor are defined before repeated use. Main lifecycle precedes exceptions. Memory and execution prerequisites link to real lessons. Reasoning checks ask readers to transfer the model to lock and CPU bottlenecks.
- Next: Lesson 20 preview `synchronization-internals`, HotSpot synchronization internals / lightweight locking / mark-word behavior in JDK 25. Shared metadata handles previous/next and sections; no separate sidebar registry.

- Final verification: `npm run check` reports 0 errors/warnings/hints; `npm run build` produces 78 pages. A transient content-loader duplicate-ID notice appeared during concurrent dev/check; one source file and one byte-019 exist, the build is clean, and the course has one Lesson 19. Production route, HTML-link and sitemap checks exclude Lesson 19 and the later preview.
- Chromium review passed at 1440, 1024, 768, 390 and 320px in light/dark mode: all 11 SVGs load and parse, no SVG text falls outside its viewBox, no page overflow, three highlights/two callouts, sidebar selection, all 12 section anchors, first/last navigation, Lesson 18 → 19 → 20, overview section counts 12/5/3, and keyboard source/reasoning/overview/mobile-contents disclosures. Comparison-table ArrowRight scrolling was verified; Java code and the table scroll internally on narrow screens. Visually inspected all diagrams, desktop/mobile light/dark typography, emphasis and tables; refined three diagram connectors.
- Added reusable opt-in comparison table styles and wrapping for long inline API names in `src/styles/learning.css`; future lessons can use the documented semantic table wrapper. Other course structure and existing lesson text are unchanged.
- Local server left running: http://127.0.0.1:4344/courses/jvm/platform-vs-virtual-threads ; overview http://127.0.0.1:4344/courses/jvm ; next preview http://127.0.0.1:4344/courses/jvm/synchronization-internals . Changes remain uncommitted; no push or deployment.
